symfonycasts_reset_password:
    request_password_repository: App\Repository\ResetPasswordRequestRepository
    # The link in the e-mail works for an hour
    lifetime: 3600
    # One e-mail per account per 15 minutes; asking again silently sends nothing
    throttle_limit: 900
